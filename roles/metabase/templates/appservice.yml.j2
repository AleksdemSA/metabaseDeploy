---
apiVersion: v1
kind: Service
metadata:
  name: metabase
  namespace: {{ NAMESPACE }}
  labels:
    app: metabase
spec:
  ports:
    - port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: metabase
